import { ScrollToCurrentDirective } from './scroll-to-current.directive';

describe('ScrollToCurrentDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToCurrentDirective();
    expect(directive).toBeTruthy();
  });
});
